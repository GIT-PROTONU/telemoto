# MoveIt2 planning pipeline config for real-robot mode.
#
# move_group expects:
#   planning_pipelines: [list of names]
#   default_planning_pipeline: <name>
#   <name>:
#     planning_plugins: [...]
#     request_adapters: [...]
#     response_adapters: [...]
#
# The upstream ompl_planning.yaml puts these keys at the top level, which
# move_group ignores — it only looks inside the pipeline-name namespace.

planning_pipelines:
  - ompl

default_planning_pipeline: ompl

ompl:
  planning_plugins:
    - ompl_interface/OMPLPlanner
  request_adapters:
    - default_planning_request_adapters/ResolveConstraintFrames
    - default_planning_request_adapters/ValidateWorkspaceBounds
    - default_planning_request_adapters/CheckStartStateBounds
    - default_planning_request_adapters/CheckStartStateCollision
  response_adapters:
    - default_planning_response_adapters/AddTimeOptimalParameterization
    - default_planning_response_adapters/ValidateSolution
    - default_planning_response_adapters/DisplayMotionPath
